#!/bin/sh
# Android Studio can import this project directly. Generate wrapper locally if needed.
echo "Use Android Studio > Open on this folder, or install Gradle 8.7 and run ./gradlew assembleDebug."
